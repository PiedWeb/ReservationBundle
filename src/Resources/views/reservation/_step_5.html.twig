<p><small>En passant votre commande, vous acceptez les <a href="{{ path('piedweb_cms_page', {slug: 'cgv'}) }}">conditions générales de vente</a>.</small></p>
<div class="row text-left">
    <div class="col-sm-12 col-md-7">
        <h2 class="mb-40">Récapitulatif</h2>

        {% include '@PiedWebReservation/reservation/_table_basketItems.html.twig' %}

        <div class="mb-10">
            <h3 class="mt-20 mb-10">Informations</h3>
            Une convocation avec l'heure et le lieu de rendez-vous vous sera automatiquement transmise à l'issu du paiement.
        </div>


    </div>
    <div class="col-sm-12 col-md-5 align-self-start" style="border: 2px solid rgba(0,0,0, 0.5); padding: 20px; border-radius:15px;">


            <p class="mt-40 info font-weight-bold">Montant total : <span class="float-right d-block">EUR {{ basket.price }}</span></p>
        <hr>

        <h3 class="mt-40 mb-40 text-center">Choisir un moyen de paiement</h3>


        {{ form_start(form) }}
        <div class="mb-40 mt-40">

            {% for paymentMethod in paymentMethods %}
                {% include '@PiedWebReservation/PaymentMethod/' ~ paymentMethod.humanId ~ '.html.twig' %}
                <br>
            {% endfor %}

        </div>
        {{ form_widget(form._token) }}
        </form>

    </div>

</div>
